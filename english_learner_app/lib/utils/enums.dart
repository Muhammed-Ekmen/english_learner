part of '../utils/extensions.dart';

enum CoreStorageKeys { firstRun, isLogin }

enum CallLotties { google, listening, reading, chat }

enum CustomColor { rossoCorsa, corduroy, deepRacingGreen, athensGray, pumpkin, greenHaze }

enum AppScreens { splash, onBoard, home, login, signIn }

enum SupportedPlatforms { apple, google, facebook }

enum UserInfoTextFieldKey { email, password }

enum WillPopBehaviour { back, alert }

enum MagicButtonKeys { logOut, info, setting }

enum CallImages { city, defaultProfile }

enum ConstSizes { screenHorizantal ,cardVertical}

enum ConstTextStyles { title, subTitle, content, description }

enum PuzzleMenuItems{read,listen,translate,lyric,movie,chat}