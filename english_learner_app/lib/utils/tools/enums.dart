import 'package:flutter/material.dart';
import 'package:font_awesome_flutter/font_awesome_flutter.dart';
import 'package:get/get.dart';
import 'package:lottie/lottie.dart';
part '../tools/extensions.dart';

enum CoreStorageKeys { firstRun, isLogin }

enum CallLotties { google, listening, reading, chat }

enum IColors { rossoCorsa, corduroy, deepRacingGreen, athensGray, pumpkin, greenHaze }

enum IRoutes { splash, onBoard, home, login, signIn }

enum SupportedPlatforms { apple, google, facebook }

enum UserInfoTextFieldKey { email, password }

enum IDeviceBehaviour { back, alert, nix }

enum MagicButtonKeys { logOut, info, setting }

enum CallImages { city, defaultProfile }

enum IMeasures { screenOrientation, cardVertical }

enum ITextStyles { demi, short, tall, grande, venti, trenta }

enum PuzzleMenuItems { read, listen, translate, lyric, movie, chat }

enum IDurations { grande, demi, short, tall }

enum ITools { divider }
